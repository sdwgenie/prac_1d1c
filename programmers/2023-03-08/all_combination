def solution(numbers):
    answer = []
    
    while numbers :
        add = numbers.pop()
        
        for i in numbers :
            answer.append(i+add)
            
    answer = sorted(list(set(answer)))
    
    return answer
